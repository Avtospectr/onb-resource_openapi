{
  "name": "onb-api-rest",
  "version": "0.1.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.125"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist/bundle.yaml && cp assets/* -r dist",
    "test": "redocly lint"
  }
}